
// Configuration for user authorization-related feature flags
export const userAuth = {
    registrationEnabled: true, // set false to temporarily disable new user registrations
    loginEnabled: true,
    profileUpdateEnabled: true,
}
